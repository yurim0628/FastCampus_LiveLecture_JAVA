package kr.fc.poly2;

public interface Pet { // 객체를 생성할 수 없다.
   /* public static void play(){ // Pet.play() } */
    /* 해당 주석의 내용은 인터페이스 내에서 정적(static) 메서드를 선언하려는 시도를 보여준다. */
    /* 정적 메서드는 인스턴스 생성과 상관없이 클래스 이름을 사용하여 호출할 수 있다. */
    /* 그러나 인터페이스의 메서드는 기본적으로 추상 메서드이며, 구현을 가지지 않는다. */
    /* 정적 메서드도 마찬가지로 구현이 가능하지만, 인터페이스에서 직접 정적 메서드를 정의할 수 없다. */
    /* 정적 메서드를 인터페이스 내에 정의하고자 한 경우에는 해당 주석과 같이 정의할 수 없음을 나타낸다. */
    /* 대신, 정적 메서드를 포함하는 클래스를 별도로 정의하여 사용해야 한다. */

    // play 메서드를 선언한다. 이 인터페이스를 구현하는 클래스에서 이 메서드를 반드시 구현해야 한다.
    public void play();
}
