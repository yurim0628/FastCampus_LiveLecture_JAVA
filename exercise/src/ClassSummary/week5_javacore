/* 6일차 */
1. 자바의 최상위 클래스는 무엇인가.
- java.lang.Object

2. 자바에서 다중상속을 지원하기 위해서 사용하는것은 무엇인가.
- 인터페이스(Interface)를 사용한다.

3. Object Casting이란 무엇인지 설명하시오.
- 객체 캐스팅(Object Casting)은 한 타입의 객체를 다른 타입으로 변환하는 것을 의미한다. 
- 업캐스팅(UpCasting)과 다운캐스팅(DownCasting)이 존재한다.

4. 다형성이론의 전제조건이 무엇인지 기술하시오.
- 상속(Inheritance)과 메서드 오버라이딩(Method Overriding)

5. 자바에서 데이터베이스를 연결하기위해서 밴더사에서 제공하는 무엇이 있어야 하는가.
- JDBC(Java Database Connectivity)를 사용해야 한다.

6. 추상클래스와 인테페이스의 공통점과 차이점을 설명하시오.
- 공통점: 추상 메서드를 가질 수 있으며, 다른 클래스에서 상속하거나 구현해야 한다.
- 차이점: 추상 클래스는 일부 구현을 가질 수 있지만 인터페이스는 모든 메서드가 추상이다.

7. 하위클래스 생성자메서드에서 상위클래스의 생성자메서드를 호출하는 방법을 기술하시오.
- super() 키워드를 사용한다.

8. 자신의 생성자 메서드에서 다른 생성자메서드를 호출하는 방법을 기술하시오.
- this() 키워드를 사용한다.

9. 하위클래스에는 public이며 다를 클래스에는 private처럼 동작하는 접근제한자를 쓰시오.
- 하위 클래스에는 protected 접근 제한자를 사용하며 다른 클래스에는 private 처럼 동작하는 접근 제한자는 package-private 이다.

10. 하위클래스의 동작을 숨기고 상위클래스 타입으로 하위클래스를 동작시키는 클래스 설계방법의 이점이 무엇인가.
- 상위 클래스 타입으로 하위 클래스를 동작시키는 클래스 설계 방법의 이점은 유연성과 확장성을 향상시킬 수 있다.
- 코드를 수정하지 않고도 새로운 하위 클래스를 추가하거나 변경할 수 있다.
===============================================================================================================
1. Collection API중에서 Object[]배열을 가지고 있으면서 모든 객체를 저장하고 출력할 수 있는 배열처럼 동작하는 API는 무엇인가.
- ArrayList는 Collection API 중에서 Object 배열을 가지고 있으면서 모든 객체를 저장하고 출력할 수 있는 배열처럼 동작하는 API이다.

2. 키보드로 부터 데이터를 입력 받아서 최종적으로 활용하기 위해서 데이터를 어떻게 만들어 놓는 것이 좋은지 설명하시오.

3. IO에서 스트림(Stream)이란 무엇인지 설명하시오.
- 데이터의 연속적인 흐름을 나타내는 개념이다.
- 데이터를 입출력하기 위한 추상적인 방법을 제공하며, 스트림은 데이터를 바이트 또는 문자 단위로 읽고 쓰는 작업을 처리하는 데 사용된다.

4. 자바에서 표준 입출력 스트림을 만드는 방법을 설명하시오.
- System.in: 표준 입력 스트림으로, 키보드 입력을 읽는다.
- System.out: 표준 출력 스트림으로, 화면에 출력한다.
- System.err: 표준 에러 출력 스트림으로, 오류 메시지를 출력한다.

5. ArrayList에 Movie객체만 저장 하도록 일반화 시키는 코드를 기술하시오.
-  List<Movie> movieList = new ArrayList<Movie>();

6. 자바 개발자들이 API를 사용하는 유형을 3가지 경우로 설명하시오.(어떤 종류의 API를 사용하는지)
- 표준 라이브러리 API: 자바 표준 라이브러리 (Java Standard Library)에 포함된 클래스 및 메서드들을 사용하는 경우. 
( 예: java.util, java.io 등. )
- 외부 라이브러리 API: 서드파티 라이브러리나 프레임워크의 API를 사용하는 경우.
( 예: Apache Commons 라이브러리, Spring 프레임워크 등. )
- 웹 서비스 API: HTTP 기반의 웹 서비스를 사용하는 경우. 
( 예: RESTful API, SOAP API 등. )

7. 배열은 삽입, 삭제가 빈번하게 이루어지는 데이터를 처리하기에는 부적합한 이유를 설명하고 더 좋은 방법의 자료구조가 무엇인지 설명하시오.
- 배열의 크기를 변경하기 어렵고 삽입, 삭제 시 다른 요소들을 이동시켜야 하기 때문이다.
- 대신 링크드 리스트(linked list)와 같은 자료구조가 더 적합하다.
- 링크드 리스트는 각 요소가 다음 요소를 가리키는 방식으로 구현되어 있어 삽입, 삭제 작업이 상대적으로 효율적으로 처리된다.

8. 특정 서버(웹)에 있는 데이터를 수집하는 기술을 무엇이라고 하는가.
- 웹 크롤링 (Web Crawling)

9. 구글,네이버,카카오톡 같은 포털에서 제공하는 데이터를 얻기 위해서 사용하는 API를 무엇이라고 하는가.
- 웹 크롤링 API

10. 9번에서 사용하는 API로 만들어 볼 수 있는 서비스가 어떤 것들이 있는지 생각해보시오.
